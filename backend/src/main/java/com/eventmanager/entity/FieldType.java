package com.eventmanager.entity;

// Enum representing the available field types in the form builder
public enum FieldType {
    TEXT,
    TEXTAREA,
    SELECT,
    CHECKBOX,
    DATE,
    EMAIL,
    PHONE,
    NUMBER
}